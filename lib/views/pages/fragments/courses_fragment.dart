import 'package:flutter/material.dart';

class CoursesFragment extends StatelessWidget {
  const CoursesFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Kelas"),
    );
  }
}
